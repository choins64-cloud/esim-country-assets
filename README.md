# eSIM Country Assets

Public country images and attribution metadata for the eSIM service.

- `images/{country-code}.jpg`: rendered country image
- `metadata/{country-code}.json`: attribution and source information
- `manifest.json`: complete country asset manifest

The untouched Unsplash downloads are not published in this repository. Attribution details remain available with every image.
